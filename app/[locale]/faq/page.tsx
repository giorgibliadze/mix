import type{Metadata}from"next";import{notFound}from"next/navigation";import{isLocale,getDictionary}from"@/lib/i18n";import{localizedMetadata}from"@/lib/seo";import{Header}from"@/components/Header";import{Footer}from"@/components/Footer";import{FaqAccordion}from"@/components/FaqAccordion";import{JsonLd}from"@/components/JsonLd";type P={params:Promise<{locale:string}>};
export async function generateMetadata({params}:P):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return localizedMetadata(locale,"faq",`FAQ | Next-Hub Solutions`,d.faqPage.items[0].answer)}
export default async function Page({params}:P){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><Header locale={locale}/><JsonLd data={{"@context":"https://schema.org","@type":"FAQPage",mainEntity:d.faqPage.items.map(item=>({"@type":"Question",name:item.question,acceptedAnswer:{"@type":"Answer",text:item.answer}}))}}/><main className="mx-auto max-w-4xl px-4 py-20 sm:px-6"><p className="eyebrow">FAQ</p><h1 className="mt-5 text-4xl font-semibold sm:text-6xl">{d.faqPage.title}</h1><div className="mt-10"><FaqAccordion items={d.faqPage.items}/></div></main><Footer locale={locale}/></>}

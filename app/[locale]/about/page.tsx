import type{Metadata}from"next";import{notFound}from"next/navigation";import{isLocale,getDictionary}from"@/lib/i18n";import{localizedMetadata}from"@/lib/seo";import{Header}from"@/components/Header";import{Footer}from"@/components/Footer";type P={params:Promise<{locale:string}>};
export async function generateMetadata({params}:P):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return localizedMetadata(locale,"about",`${d.common.about} | Next-Hub Solutions`,d.about.description)}
export default async function Page({params}:P){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><Header locale={locale}/><main className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8"><p className="eyebrow">Next-Hub Solutions</p><h1 className="mt-6 max-w-4xl text-4xl font-semibold sm:text-6xl">{d.about.title}</h1><p className="mt-8 max-w-3xl text-xl leading-9 text-slate-400">{d.about.description}</p><div className="mt-14 grid gap-5 md:grid-cols-3">{d.about.items.map((item,index)=><article className="industry-card" key={item}><span className="text-[#F13024]">0{index+1}</span><h2 className="mt-5 text-xl font-semibold">{item}</h2></article>)}</div></main><Footer locale={locale}/></>}
